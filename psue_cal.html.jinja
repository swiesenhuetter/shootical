<!DOCTYPE html>
<html>
<head>
<style>
table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
</style>
</head>
<body>

<h2>PSUE Schiesskalender</h2>

<table>
  <tr>
    <th>Datum</th>
    <th>Uhrzeit</th>
    <th>Anlass</th>
  </tr>

  {% for event in events %}
  <tr>
      <td>{{ event.date }}</td>
      <td>{{ event.time }}</td>
      <td>{{ event.descr }}</td>
  </tr>
  {% endfor %}
</table>

</body>
</html>